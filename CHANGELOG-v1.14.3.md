# InvitaPro v1.14.3

## Corrección

- Corrige el error de TypeScript en `lib/template-engine.ts`.
- Los presets ya no requieren declarar `hero`, `motion` y `radius` porque esos valores se calculan dentro de `resolveTemplateEngine()`.
- No cambia la apariencia ni la lógica de las plantillas.
- No requiere cambios en Supabase.
