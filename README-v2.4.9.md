# InvitaPro v2.4.9 HOTFIX — visibilidad garantizada en /solicitar

Corrige de forma directa:
- Logo visible usando marca `IP` ciruela dentro del propio encabezado.
- Botón principal con estilos inline para evitar que reglas CSS heredadas lo vuelvan blanco.
- Texto del botón siempre blanco y fondo ciruela.

No requiere SQL ni copiar CSS manualmente.

Aplicar el patch, reiniciar `pnpm dev` y hacer Ctrl+F5 en `/solicitar`.
